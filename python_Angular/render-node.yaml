services:
  - type: web
    name: python-angular-app
    env: node
    buildCommand: npm install && npm run build:ssr
    startCommand: npm run serve:ssr
    plan: free
    envVars:
      - key: NODE_ENV
        value: production